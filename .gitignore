# Rust
target/

# Node / Next.js
node_modules/
.next/
out/
web/node_modules/
web/.next/
web/out/
web/data/block-hashes.json
web/public/data/block-hashes/
web/*.zip
*.tsbuildinfo
# Fumadocs build/source cache — regenerated on `next dev`
web/.source/
# Vendored confidential-transfers SDK (cloned by scripts/vendor-confidential-transfers.sh)
web/vendor/

# Sui build artifacts
contracts/build/
contracts/.sui/

# Environment / secrets
.env
*.env.local
.env.*
!.env.example
Pub.*.toml
*.key
*.keystore
*keypair*
# Generated signet test wallet — contains a WIF private key
.env.btc-signet
# Sui CLI keystore + config (machine-local)
.sui/
sui_config/

# Generated backups
*.bak

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Runtime logs (may contain RPC URLs or live mining data)
logs/
*.log

# Local-only AI tooling guidance — not part of the protocol
CLAUDE.md
